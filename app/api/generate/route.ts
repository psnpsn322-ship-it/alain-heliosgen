import { NextRequest, NextResponse } from "next/server";
import https from "node:https";
import http from "node:http";
import { spawn } from "node:child_process";
import { writeFile, unlink, readFile } from "node:fs/promises";
import { tmpdir } from "node:os";
import { join } from "node:path";
import { jobStore } from "@/lib/jobStore";
import { ensureR2, uploadBuffer } from "@/lib/r2";
import { supabaseAdmin } from "@/lib/supabase/admin";
import { IMAGE_MODELS, validateAzureCustomSize } from "@/lib/modelConfig";
import { getKieTokenForUser } from "@/lib/getKieToken";
import { getAzureKeyForUser } from "@/lib/getAzureKey";
import { GUEST_MODE, resolveUserId } from "@/lib/guestMode";
import * as guestDb from "@/lib/guest/db";

const BASE   = "https://api.kie.ai";
const CREATE = `${BASE}/api/v1/jobs/createTask`;

function httpsPost(url: string, headers: Record<string, string>, body: string, timeoutMs = 295_000): Promise<{ ok: boolean; status: number; text: () => Promise<string> }> {
  return new Promise((resolve, reject) => {
    const u = new URL(url); const bodyBuf = Buffer.from(body, "utf8");
    const req = https.request({hostname:u.hostname,port:u.port?Number(u.port):443,path:u.pathname+u.search,method:"POST",headers:{...headers,"Content-Length":bodyBuf.byteLength}},(res)=>{const chunks:Buffer[]=[];res.on("error",reject);res.on("data",(c:Buffer)=>chunks.push(c));res.on("end",()=>{const raw=Buffer.concat(chunks).toString("utf8");resolve({ok:(res.statusCode??0)>=200&&(res.statusCode??0)<300,status:res.statusCode??0,text:()=>Promise.resolve(raw)});});});
    req.on("socket",socket=>{socket.setNoDelay(true);socket.setKeepAlive(true,10_000);socket.setTimeout(timeoutMs,()=>req.destroy(new Error(`Azure request timed out after ${timeoutMs/1000}s`)));});req.on("error",reject);req.write(bodyBuf);req.end();
  });
}
function fetchBuffer(url:string,maxRedirects=5):Promise<Buffer>{return new Promise((resolve,reject)=>{if(maxRedirects<=0)return reject(new Error("Too many redirects"));const u=new URL(url);const mod=u.protocol==="https:"?https:(http as unknown as typeof https);mod.get(url,res=>{if(res.statusCode&&res.statusCode>=300&&res.statusCode<400&&res.headers.location)return fetchBuffer(res.headers.location,maxRedirects-1).then(resolve).catch(reject);if(!res.statusCode||res.statusCode<200||res.statusCode>=300)return reject(new Error(`HTTP ${res.statusCode} fetching image`));const chunks:Buffer[]=[];res.on("data",(c:Buffer)=>chunks.push(c));res.on("end",()=>resolve(Buffer.concat(chunks)));res.on("error",reject);}).on("error",reject);});}
async function curlMultipartPost(url:string,authKey:string,images:Array<{buf:Buffer;mime:string;ext:string}>,textFields:Record<string,string>):Promise<{ok:boolean;status:number;body:string}>{const tmpFiles:string[]=[];const bodyPath=join(tmpdir(),`azure-resp-${Date.now()}.json`);try{for(const img of images){const p=join(tmpdir(),`azure-img-${Date.now()}-${Math.random().toString(36).slice(2)}.${img.ext}`);await writeFile(p,img.buf);tmpFiles.push(p);}const args=["-s","-m","290","-X","POST",url,"-H",`Authorization: Bearer ${authKey}`,"-o",bodyPath,"-w","%{http_code}"];for(let i=0;i<images.length;i++)args.push("-F",`image[]=@${tmpFiles[i]};type=${images[i].mime}`);for(const[k,v]of Object.entries(textFields))args.push("-F",`${k}=${v}`);const runCurl=()=>new Promise<{statusStr:string;stderr:string;exitCode:number}>((resolve,reject)=>{let out="",err="";const proc=spawn("curl",args);proc.stdout.on("data",(d:Buffer)=>out+=d.toString());proc.stderr.on("data",(d:Buffer)=>err+=d.toString());proc.on("close",code=>resolve({statusStr:out.trim(),stderr:err,exitCode:code??-1}));proc.on("error",e=>reject(new Error(`curl spawn failed: ${e.message}`)));});let statusStr="",stderr="",exitCode=-1;for(let attempt=1;attempt<=3;attempt++){({statusStr,stderr,exitCode}=await runCurl());if(exitCode!==35)break;if(attempt<3)await new Promise(r=>setTimeout(r,1000*attempt));}if(exitCode!==0)throw new Error(`Azure curl request failed: ${exitCode===28?"timed out (Vercel Hobby limit)":`curl exited with code ${exitCode}`}`);const status=parseInt(statusStr,10)||0;const body=await readFile(bodyPath,"utf-8").catch(()=>"");return{ok:status>=200&&status<300,status,body};}finally{for(const f of[...tmpFiles,bodyPath])unlink(f).catch(()=>{});}}
async function resolveImages(imageUrls:string[]):Promise<string[]>{const resolved=await Promise.all(imageUrls.slice(0,14).map(u=>ensureR2(u,"references").catch(()=>null)));return resolved.filter((u):u is string=>u!==null);}
const CODEX_SIZE_MAP:Record<string,string>={auto:"auto","1:1":"1024x1024","16:9":"1536x1024","9:16":"1024x1536","4:3":"1536x1024","3:4":"1024x1536"};
function cleanCodexError(raw:string):string{const match=raw.match(/Error:\s*([\s\S]*)$/);const tail=(match?match[1]:raw).trim();const braceIdx=tail.indexOf("{");if(braceIdx!==-1){try{const parsed=JSON.parse(tail.slice(braceIdx));const message=parsed?.error?.message;if(typeof message==="string"&&message)return message;}catch{}}const semiIdx=tail.indexOf(";");return(semiIdx!==-1?tail.slice(0,semiIdx).trim():tail)||raw;}
async function runCodexImagegen(opts:{prompt:string;images:Array<{buf:Buffer;ext:string}>;size:string}):Promise<Buffer>{const tmpFiles:string[]=[];const outPath=join(tmpdir(),`codex-out-${Date.now()}-${Math.random().toString(36).slice(2)}.png`);try{const imagePaths:string[]=[];for(const img of opts.images){const p=join(tmpdir(),`codex-in-${Date.now()}-${Math.random().toString(36).slice(2)}.${img.ext}`);await writeFile(p,img.buf);tmpFiles.push(p);imagePaths.push(p);}const args=imagePaths.length>0?["edit",...imagePaths.flatMap(p=>["--image",p]),"--prompt",opts.prompt,"--size",opts.size,"--out",outPath,"--force"]:["generate","--prompt",opts.prompt,"--size",opts.size,"--out",outPath,"--force"];const{exitCode,stderr}=await new Promise<{exitCode:number;stderr:string}>((resolve,reject)=>{let err="";const proc=spawn("codex-imagegen",args);proc.stderr.on("data",(d:Buffer)=>err+=d.toString());proc.on("close",code=>resolve({exitCode:code??-1,stderr:err}));proc.on("error",e=>reject(new Error(`codex-imagegen spawn failed: ${e.message}`)));});if(exitCode!==0)throw new Error(`codex-imagegen exited with code ${exitCode}: ${stderr.slice(0,500)||"no stderr output"}`);return await readFile(outPath);}finally{for(const f of[...tmpFiles,outPath])unlink(f).catch(()=>{});}}

// Vercel Hobby accepts 1..300 seconds. Keep a small internal safety margin in network calls.
export const maxDuration = 300;

export async function POST(req:NextRequest){
 const{model="nano-banana-2",prompt,imageUrls=[],aspectRatio="1:1",quality="1k",azureQuality,azureResolution,azureBaseUrl,azureDeployment,azureCustomWidth,azureCustomHeight,codexProvider,debugOnly}=(await req.json()) as {model?:string;prompt?:string;imageUrls?:string[];aspectRatio?:string;quality?:string;azureQuality?:string;azureResolution?:string;azureBaseUrl?:string;azureDeployment?:string;azureCustomWidth?:number;azureCustomHeight?:number;codexProvider?:boolean;debugOnly?:boolean};
 if(debugOnly)return NextResponse.json({ok:true});if(!prompt?.trim())return NextResponse.json({error:"Prompt is required"},{status:400});const cfg=IMAGE_MODELS.find(m=>m.id===model);if(!cfg)return NextResponse.json({error:`Unknown model: ${model}`},{status:400});let r2ImageUrls:string[]=[];try{r2ImageUrls=await resolveImages(imageUrls);}catch{}const currentUserId=await resolveUserId(req).catch(()=>null);
 if(azureBaseUrl&&azureDeployment){const azureKey=currentUserId?await getAzureKeyForUser(currentUserId):process.env.AZURE_API_KEY??null;if(!azureKey)return NextResponse.json({error:"Azure API key is not configured. Add it in Settings."},{status:500});const resSizeMaps=cfg.azureResolutionSizeMaps??{};const sizeMap=azureResolution&&resSizeMaps[azureResolution]?resSizeMaps[azureResolution]:(cfg.azureSizeMap??{});const customSizeError=aspectRatio==="custom"&&azureCustomWidth&&azureCustomHeight?validateAzureCustomSize(azureCustomWidth,azureCustomHeight):null;const size=aspectRatio==="custom"&&azureCustomWidth&&azureCustomHeight&&!customSizeError?`${azureCustomWidth}x${azureCustomHeight}`:(sizeMap[aspectRatio]??"1024x1024");const aq=azureQuality||"medium",base=azureBaseUrl.replace(/\/$/,""),azureApiVersion=cfg.azureApiVersion??"2025-04-01-preview",truncatedPrompt=prompt.slice(0,cfg.apiInput.promptMaxLength??32000),azureTaskId=`azure-${Date.now()}-${Math.random().toString(36).slice(2,8)}`,azureUserId=currentUserId,hasRefImages=r2ImageUrls.length>0;jobStore.set(azureTaskId,{status:"pending",type:"image",userId:currentUserId??undefined});(async()=>{try{let res:{ok:boolean;status:number;text:()=>Promise<string>};if(hasRefImages){const azureUrl=`${base}/openai/deployments/${azureDeployment}/images/edits?api-version=${azureApiVersion}`;const images=await Promise.all(r2ImageUrls.slice(0,cfg.maxImages).map(async imgUrl=>{const buf=await fetchBuffer(imgUrl),raw=imgUrl.split("?")[0].split(".").pop()?.toLowerCase()??"png",ext=raw==="jpg"?"jpeg":raw,mime=ext==="jpeg"?"image/jpeg":"image/png";return{buf,ext,mime};}));const textFields:Record<string,string>={prompt:truncatedPrompt,quality:aq,output_format:"png",n:"1"};if(size&&size!=="auto")textFields.size=size;const curl=await curlMultipartPost(azureUrl,azureKey,images,textFields);res={ok:curl.ok,status:curl.status,text:()=>Promise.resolve(curl.body)};}else{const azureUrl=`${base}/openai/deployments/${azureDeployment}/images/generations?api-version=${azureApiVersion}`;const body:Record<string,unknown>={prompt:truncatedPrompt,n:1,output_format:"png",output_compression:100,quality:aq};if(size&&size!=="auto")body.size=size;res=await httpsPost(azureUrl,{"Content-Type":"application/json",Authorization:`Bearer ${azureKey}`},JSON.stringify(body));}const txt=await res.text();if(!res.ok){jobStore.set(azureTaskId,{status:"error",error:`Azure error ${res.status}`});return;}const azureJson=JSON.parse(txt),b64=azureJson?.data?.[0]?.b64_json as string|undefined;if(!b64){jobStore.set(azureTaskId,{status:"error",error:"Azure returned no image data"});return;}const buf=Buffer.from(b64,"base64"),imageUrl=await uploadBuffer(buf,"image/png","generated");jobStore.set(azureTaskId,{status:"done",imageUrl});const record={task_id:azureTaskId,user_id:azureUserId,generation_type:"image",status:"done",image_url:imageUrl,prompt:prompt.slice(0,2000),model,aspect_ratio:aspectRatio,quality:aq,azure_resolution:azureResolution,reference_image_urls:hasRefImages?r2ImageUrls:undefined};if(GUEST_MODE)guestDb.insertGeneration(record);else supabaseAdmin.from("generations").insert(record).then(({error})=>{if(error)console.error("[azure] supabase insert error:",error.message);});}catch(e:unknown){const msg=e instanceof Error?e.message:String(e);jobStore.set(azureTaskId,{status:"error",error:msg});}})();return NextResponse.json({taskId:azureTaskId});}
 if(codexProvider){const codexTaskId=`codex-${Date.now()}-${Math.random().toString(36).slice(2,8)}`,codexUserId=currentUserId,size=CODEX_SIZE_MAP[aspectRatio]??"auto",codexPrompt=(prompt.slice(0,cfg.apiInput.promptMaxLength??8000).replace(/<<<image (\d+)>>>/gi,(_m,n)=>`image ${n}`)+(aspectRatio&&aspectRatio!=="auto"?` Aspect ratio: ${aspectRatio}.`:"")).trim();jobStore.set(codexTaskId,{status:"pending",type:"image",userId:currentUserId??undefined});(async()=>{try{const images=await Promise.all(r2ImageUrls.slice(0,5).map(async url=>{const buf=await fetchBuffer(url),raw=url.split("?")[0].split(".").pop()?.toLowerCase()??"png",ext=raw==="jpg"?"jpeg":raw;return{buf,ext};}));const outBuf=await runCodexImagegen({prompt:codexPrompt,images,size}),imageUrl=await uploadBuffer(outBuf,"image/png","generated");jobStore.set(codexTaskId,{status:"done",imageUrl});const record={task_id:codexTaskId,user_id:codexUserId,generation_type:"image",status:"done",image_url:imageUrl,prompt:prompt.slice(0,2000),model,aspect_ratio:aspectRatio,quality};if(GUEST_MODE)guestDb.insertGeneration(record);else supabaseAdmin.from("generations").insert(record).then(({error})=>{if(error)console.error("[codex] supabase insert error:",error.message);});}catch(e:unknown){const msg=e instanceof Error?e.message:String(e);jobStore.set(codexTaskId,{status:"error",error:cleanCodexError(msg)});}})();return NextResponse.json({taskId:codexTaskId});}
 const kieToken=currentUserId?await getKieTokenForUser(currentUserId):null;if(!kieToken)return NextResponse.json({error:"No Kie.ai API key configured. Add one in Settings."},{status:401});const callbackBase=process.env.CALLBACK_BASE_URL;if(!callbackBase)return NextResponse.json({error:"CALLBACK_BASE_URL is not set"},{status:500});const callBackUrl=`${callbackBase.replace(/\/$/,"")}/api/callback`;
 try{const{apiInput}=cfg,hasImages=r2ImageUrls.length>0,resolvedApiId=!hasImages&&cfg.textOnlyApiId?cfg.textOnlyApiId:cfg.apiId,input:Record<string,unknown>={prompt:prompt.slice(0,apiInput.promptMaxLength),[apiInput.aspectRatioKey]:aspectRatio};if(apiInput.outputFormat)input.output_format=apiInput.outputFormat;if(apiInput.imageInputKey&&hasImages)input[apiInput.imageInputKey]=r2ImageUrls.slice(0,cfg.maxImages);if(apiInput.qualityKey)input[apiInput.qualityKey]=apiInput.qualityMap?(apiInput.qualityMap[quality]??quality):quality==="4k"?"4K":quality==="2k"?"2K":quality==="1k"?"1K":quality;if(apiInput.extra)Object.assign(input,apiInput.extra);const res=await fetch(CREATE,{method:"POST",headers:{Authorization:`Bearer ${kieToken}`,"Content-Type":"application/json"},body:JSON.stringify({model:resolvedApiId,callBackUrl,input})});if(!res.ok){if(res.status===401)throw new Error("Invalid Kie.ai API key — please update it in Settings.");throw new Error(await res.text());}const d=await res.json();if(d.code!==undefined&&d.code!==200)throw new Error(d.msg??`API error ${d.code}`);const taskId=d.data?.taskId??d.data?.id??d.taskId??d.id;if(!taskId)throw new Error("No task ID in response");jobStore.set(taskId,{status:"pending",userId:currentUserId??undefined});const record={task_id:taskId,user_id:currentUserId,generation_type:"image",status:"pending",prompt,model,aspect_ratio:aspectRatio,quality,reference_image_urls:r2ImageUrls};if(GUEST_MODE)guestDb.insertGeneration(record);else supabaseAdmin.from("generations").insert(record).then(({error})=>{if(error)console.error("[generate] supabase insert error:",error.message);});return NextResponse.json({taskId,referenceImageUrls:r2ImageUrls});}catch(e:unknown){const msg=e instanceof Error?e.message:String(e);return NextResponse.json({error:msg},{status:500});}
}
